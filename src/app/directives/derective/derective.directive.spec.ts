import { DerectiveDirective } from './derective.directive';

describe('DerectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DerectiveDirective();
    expect(directive).toBeTruthy();
  });
});
